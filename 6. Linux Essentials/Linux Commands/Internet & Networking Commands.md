```bash

ip a                          # IP and interface info

ping google.com               # Ping test

ping -c 4 google.com          # Send 4 packets

netstat -tulpn                # Listening ports

netstat -tn                   # TCP connections

netstat -in                   # Interface stats

netstat -tuln                 # TCP/UDP ports

arp -a                        # Show ARP table

nmap 192.168.1.1              # Scan host

```

---